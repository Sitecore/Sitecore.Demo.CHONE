import { View } from "react-native";
import { Text } from "react-native-paper";

export const CreateAthleteScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Create Athlete!</Text>
    </View>
  );
};
